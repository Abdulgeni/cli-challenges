# CLI Challenges

A collection of command-line utility scripts with tests.

## Scripts

- `file_organizer.py` — Organize files in a directory by extension

## Usage

```bash
python file_organizer.py /path/to/folder